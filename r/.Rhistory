f = file.path(DIR_Repo. "acc_ids.tbl")
f = file.path(DIR_Repo, "acc_ids.tbl")
t = read.table(f, sep="\t")
t
ids_all = sprintf("HM%0d", c(1:339))
ids_all
t
tt = cbind(id = ids_all, acc_all=1)
t2 = merge(t, tt, by="id", all=T)
head(t)
t=read.table(f, header=T, sep="\t")
head(t)
t2 = merge(t, tt, by="id", all=T)
head(t2)
nrow(t2)
t=read.table(f, header=T, sep="\t", stringsAsFactors=F)
t2 = merge(t, tt, by="id", all=T)
norw(t2)
nrow(t2)
t2
ids_all = sprintf("HM%03d", c(1:339))
tt = cbind(id = ids_all, acc_all=1)
t2 = merge(t, tt, by="id", all=T)
t2
ids_all = sprintf("HM%03d", c(1:339))
ids_all
tt = cbind(id=ids_all, acc_all=1)
t2 = merge(t, tt, by="id", all=T)
t2
write.table(t2, file.path(DIR_Repo, "acc.tbl"), sep="\t", row.names=F, col.names=T, quote=F, na="")
q()
